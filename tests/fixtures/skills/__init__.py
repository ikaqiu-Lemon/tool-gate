"""Fixture package marker for mock skills (Stage A skeleton).

Each subdirectory (``mock_*``) contains a single ``SKILL.md`` consumed by
``SkillIndexer``. This ``__init__`` exists only so pytest discovery and
tooling treat the tree uniformly; the indexer does not require it.
"""
